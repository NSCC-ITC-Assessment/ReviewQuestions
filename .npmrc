# Enforce pnpm as the only allowed package manager.
# Running `npm install` or `yarn` will be blocked by the preinstall script.
# Install pnpm via Corepack: `corepack enable && corepack prepare pnpm@latest --activate`
engine-strict=true
